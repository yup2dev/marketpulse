# 프로덕션 백엔드 서버 URL
# CI/Vercel 환경변수의 VITE_API_URL 이 이 파일보다 우선 적용된다.
#
# src/config/api.js 의 폴백은 http://localhost:8000 이므로, 프로덕션 빌드에서는
# 이 값(또는 CI 환경변수)이 반드시 실제 서버를 가리켜야 한다.
VITE_API_URL=https://api.finance.dns-co.kr
