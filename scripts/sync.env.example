# scripts/sync.env 로 복사한 뒤 값을 채우세요 (sync.env는 git에 커밋 금지)
# daily_ingest.bat / sync_db_to_cloud.py 가 읽는 클라우드 동기화 인증 정보
MP_SYNC_EMAIL=admin@example.com
MP_SYNC_PASSWORD=your-password
# 필요 시 대상 백엔드 변경 (기본: https://api.finance.dns-co.kr)
# MP_SYNC_BASE_URL=http://127.0.0.1:8000
